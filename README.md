# Weather-app
A desktop weather app built with Python and PyQt5. Enter any city name to instantly get the current temperature, weather condition, and a matching emoji — powered by the OpenWeatherMap API.
